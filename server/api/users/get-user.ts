import { createPool, sql, db } from '@vercel/postgres'

// const client = await db.connect();
// await client.sql`SELECT userId FROM users WHERE username= ${username} AND password= ${password}`;

// const username = 

// const { userId } = await client.sql`SELECT userId FROM users WHERE username= ${username} AND password= ${password}`;
